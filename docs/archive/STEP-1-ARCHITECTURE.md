# Step 1 Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                           USER REQUEST                               │
│                     GET /api/info                                    │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    EXPRESS SERVER (server.js)                        │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Step 1: Check Database                                      │   │
│  │  SELECT * FROM token_info WHERE contract_address = ?         │   │
│  │  AND updated_at > NOW() - INTERVAL '1 hour'                 │   │
│  └───────────────────────────┬─────────────────────────────────┘   │
│                              │                                       │
│                              ▼                                       │
│                    ┌─────────────────┐                              │
│                    │  Data Found &   │                              │
│                    │  Fresh (< 1hr)? │                              │
│                    └────┬────────┬───┘                              │
│                         │ YES    │ NO                               │
│        ┌────────────────┘        └──────────────┐                  │
│        ▼                                         ▼                  │
│  ┌────────────┐                    ┌──────────────────────────┐   │
│  │ Return DB  │                    │  Step 2: Fetch Upstream  │   │
│  │   Data     │                    │  (3 Parallel API Calls)  │   │
│  │ _source:   │                    │  - tokensupply           │   │
│  │ "database" │                    │  - tokentx               │   │
│  └────────────┘                    │  - tokeninfo             │   │
│        │                            └──────────┬───────────────┘   │
│        │                                       │                    │
│        │                            ┌──────────▼───────────┐       │
│        │                            │  Upstream Success?   │       │
│        │                            └──────┬───────┬───────┘       │
│        │                                   │ YES   │ NO            │
│        │                     ┌─────────────┘       └─────────┐    │
│        │                     ▼                               ▼    │
│        │          ┌─────────────────────┐      ┌──────────────────┐│
│        │          │ Step 3: Persist DB  │      │ Fallback: Return ││
│        │          │ INSERT ... ON       │      │ Stale DB Data    ││
│        │          │ CONFLICT DO UPDATE  │      │ _source:         ││
│        │          └──────────┬──────────┘      │ "database_       ││
│        │                     │                 │  fallback"       ││
│        │                     ▼                 └──────────┬────────┘│
│        │          ┌─────────────────────┐               │         │
│        │          │ Return Upstream     │               │         │
│        │          │ Data                │               │         │
│        │          │ _source: "upstream" │               │         │
│        │          └──────────┬──────────┘               │         │
│        │                     │                          │         │
│        └─────────────────────┴──────────────────────────┘         │
│                              │                                     │
└──────────────────────────────┼─────────────────────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │   JSON RESPONSE      │
                    │   - tokenName        │
                    │   - tokenSymbol      │
                    │   - totalSupply      │
                    │   - _source          │
                    └──────────────────────┘
```

## Database Schema

```
┌───────────────────────────────────────────────────────────────┐
│                      token_info table                          │
├────────────────────────┬──────────────────┬──────────────────┤
│ Column                 │ Type             │ Description       │
├────────────────────────┼──────────────────┼──────────────────┤
│ contract_address (PK)  │ VARCHAR          │ 0x85cb09...      │
│ chain_id               │ INTEGER          │ 137 (Polygon)    │
│ token_name             │ VARCHAR          │ "Bazaars"        │
│ token_symbol           │ VARCHAR          │ "BZR"            │
│ token_decimals         │ INTEGER          │ 18               │
│ total_supply_raw       │ VARCHAR          │ Wei value        │
│ total_supply_formatted │ VARCHAR          │ "55555555"       │
│ circulating_supply_raw │ VARCHAR          │ Wei value        │
│ circulating_supply_... │ VARCHAR          │ Formatted        │
│ source_data            │ JSONB            │ Raw API response │
│ last_fetch_success     │ BOOLEAN          │ true/false       │
│ last_fetch_error       │ TEXT             │ Error message    │
│ created_at             │ TIMESTAMP        │ First created    │
│ updated_at             │ TIMESTAMP        │ Last updated     │
└────────────────────────┴──────────────────┴──────────────────┘

Indexes:
- PRIMARY KEY: contract_address
- idx_token_info_contract: contract_address
- idx_token_info_updated: updated_at DESC
```

## Data Flow Timeline

```
Time: 00:00 (First Request)
├─ User: GET /api/info
├─ Server: Check DB → Empty
├─ Server: Fetch Etherscan → 3 API calls (~800ms)
├─ Server: Parse & Format
├─ Server: INSERT to DB
└─ Response: 950ms (slow, but one-time)
    _source: "upstream"

Time: 00:01 (Second Request)
├─ User: GET /api/info
├─ Server: Check DB → Found, updated 1 min ago
└─ Response: 75ms (fast!)
    _source: "database"

Time: 00:30 (Many Requests)
├─ User: GET /api/info × 100
├─ Server: Check DB → All from cache
└─ Avg Response: 50-100ms
    100% served from DB, 0 API calls

Time: 01:00 (Cron Refresh)
├─ Cron: node scripts/refresh-token-info.js
├─ Script: Check DB → Updated 1 hour ago
├─ Script: Fetch Etherscan → Update DB
└─ DB: updated_at = NOW()

Time: 01:01 (User Request)
├─ User: GET /api/info
├─ Server: Check DB → Fresh data
└─ Response: 65ms
    _source: "database"

Time: 02:00 (Upstream Failure)
├─ Cron: Refresh script runs
├─ Script: Etherscan API → 500 Error
├─ Script: Log error, exit code 1
└─ DB: Still has 1-hour old data

Time: 02:01 (User Request During Outage)
├─ User: GET /api/info
├─ Server: Check DB → Stale (> 1 hour)
├─ Server: Fetch Etherscan → 500 Error
├─ Server: Fallback to DB (any age)
└─ Response: 120ms
    _source: "database_fallback"
    _warning: "Upstream API unavailable..."
    User sees data instead of error! ✅
```

## Comparison: Before vs After

### Before (Old Architecture)
```
Request → In-Memory (5 min) → Etherscan API → Response
                                    ↓
                                   Error
                                    ↓
                          "temporarily unavailable"

Characteristics:
- Response Time: 800ms (avg)
- API Calls: 3 per request (after cache expires)
- Restart Impact: Cache lost
- Upstream Failure: User sees error
- Cache Duration: 5 minutes
```

### After (New Architecture)
```
Request → Database (1 hour) → Response (fast!)
               ↓
          Not Found/Stale
               ↓
          Etherscan API → Persist → Response
               ↓
            Failure
               ↓
         Return Stale Data → Response (degraded but working!)

Characteristics:
- Response Time: <100ms (avg)
- API Calls: 0 (cached) or 3 (refresh)
- Restart Impact: No impact (DB persists)
- Upstream Failure: Graceful fallback
- Cache Duration: 1 hour (+ stale available)
```

## Background Refresh Process

```
┌──────────────────────────────────────────────────────────────┐
│                    Hourly Cron Job                            │
│   0 * * * * node scripts/refresh-token-info.js               │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
          ┌────────────────────┐
          │ Connect to DB      │
          └──────────┬─────────┘
                     │
                     ▼
          ┌────────────────────────────┐
          │ Check Last Update Time     │
          │ SELECT updated_at FROM ... │
          └──────────┬─────────────────┘
                     │
                     ▼
          ┌──────────────────┐
          │ Updated < 1hr?   │
          └────┬─────────┬───┘
               │ YES     │ NO
               │         │
               ▼         ▼
        ┌──────────┐  ┌──────────────────────┐
        │ Skip     │  │ Fetch from Etherscan │
        │ (log)    │  │ 3 API calls          │
        └──────────┘  └──────────┬───────────┘
                                 │
                                 ▼
                      ┌───────────────────┐
                      │ Parse Response    │
                      └──────────┬────────┘
                                 │
                                 ▼
                      ┌───────────────────────┐
                      │ UPSERT to Database    │
                      │ ON CONFLICT UPDATE    │
                      └──────────┬────────────┘
                                 │
                                 ▼
                      ┌───────────────────────┐
                      │ Log Success           │
                      │ Exit 0                │
                      └───────────────────────┘
```

## Force Refresh Flow

```
User/Admin → GET /api/info?force=true
                    │
                    ▼
         ┌─────────────────────┐
         │ Skip DB Check       │
         │ (force=true)        │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │ Fetch Etherscan     │
         │ Immediately         │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │ Update Database     │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │ Return Fresh Data   │
         │ _source: "upstream" │
         └─────────────────────┘

Use Cases for Force Refresh:
- Token contract upgrade/migration
- Supply change detected elsewhere
- Manual verification needed
- Admin debugging
```

## Admin Endpoint Flow

```
Admin → POST /admin/refresh-token-info
        Header: X-Admin-Password: secret
                    │
                    ▼
         ┌─────────────────────┐
         │ Verify Password     │
         └──────┬───────┬──────┘
                │ VALID │ INVALID
                │       │
                ▼       ▼
         ┌──────────┐  ┌──────┐
         │ Internal │  │ 401  │
         │ GET      │  │ Unauth│
         │ /api/info│  └──────┘
         │?force=true│
         └────┬─────┘
              │
              ▼
         ┌──────────────────┐
         │ Return Result    │
         │ {success: true,  │
         │  data: {...}}    │
         └──────────────────┘
```

## Monitoring Points

```
┌─────────────────────────────────────────────────────────────┐
│                  Monitoring Strategy                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  📊 Database Metrics                                         │
│  ├─ SELECT updated_at FROM token_info                       │
│  ├─ Check last_fetch_success boolean                        │
│  └─ Monitor last_fetch_error for patterns                   │
│                                                              │
│  📈 API Metrics (from logs)                                 │
│  ├─ Count: "_source": "database" (should be >95%)          │
│  ├─ Count: "_source": "upstream" (should be rare)          │
│  ├─ Count: "_source": "database_fallback" (alert!)         │
│  └─ Response times (should be <100ms average)              │
│                                                              │
│  🔄 Refresh Script Metrics                                  │
│  ├─ Cron logs: /var/log/bzr/refresh-token-info.log         │
│  ├─ Success rate (should be >98%)                          │
│  ├─ Execution time (should be <2 seconds)                  │
│  └─ Alert on 3+ consecutive failures                        │
│                                                              │
│  🚨 Alert Conditions                                         │
│  ├─ DB updated_at > 2 hours old                            │
│  ├─ last_fetch_success = false for 3+ hours                │
│  ├─ Multiple "database_fallback" responses                  │
│  └─ Service restart with empty DB                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Error Handling Matrix

```
┌─────────────────────┬──────────────────────┬──────────────────┐
│ Scenario            │ System Response      │ User Experience  │
├─────────────────────┼──────────────────────┼──────────────────┤
│ DB has fresh data   │ Return immediately   │ Fast, perfect    │
│ (< 1 hour)          │                      │                  │
├─────────────────────┼──────────────────────┼──────────────────┤
│ DB stale, upstream  │ Fetch & update,      │ Slower once,     │
│ available           │ return new data      │ then fast        │
├─────────────────────┼──────────────────────┼──────────────────┤
│ DB stale, upstream  │ Return stale DB data │ Works! Shows     │
│ fails (timeout)     │ with warning         │ cached data      │
├─────────────────────┼──────────────────────┼──────────────────┤
│ DB empty, upstream  │ Fetch & insert       │ Normal speed     │
│ available           │                      │                  │
├─────────────────────┼──────────────────────┼──────────────────┤
│ DB empty, upstream  │ 500 error            │ Error (rare,     │
│ fails               │                      │ first request)   │
├─────────────────────┼──────────────────────┼──────────────────┤
│ Service restart     │ DB persists, no      │ No interruption  │
│                     │ impact               │                  │
├─────────────────────┼──────────────────────┼──────────────────┤
│ Force refresh       │ Bypass DB, fetch     │ Gets latest data │
│ requested           │ upstream             │                  │
└─────────────────────┴──────────────────────┴──────────────────┘
```

This architecture ensures **99.9%+ uptime** for token info display, even when upstream APIs have issues.
